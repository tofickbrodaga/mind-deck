# MindDeck - Quizlet-like Learning Application

Мощное приложение для изучения с использованием интервального повторения (SRS 2.0), вдохновленное Quizlet, но с улучшенным алгоритмом повторений.

## 🚀 Быстрый старт

```bash
make up
```

Или напрямую:
```bash
docker-compose up -d
```

```bash
source .venv/bin/activate
pytest --cov=. --cov-report=term-missing
```

- ✅ PostgreSQL (БД)
- ✅ Redis (кэш)
- ✅ Бэкенд на http://localhost:8000
- ✅ Фронтенд на http://localhost:3000


## Особенности

### Базовый функционал
- ✅ Создание наборов карточек (термин — определение)
- ✅ Режимы обучения: Карточки, Множественный выбор, Письмо, Подбор
- ✅ Импорт из Word, Excel, изображений (OCR)
- ✅ Text-to-Speech на 50+ языках
- ✅ Алгоритм интервального повторения (FSRS)

### Уникальные возможности
- 👆 Управление жестами (свайпы как в Tinder)

## Архитектура

Приложение построено на принципах Clean Architecture:
- **Domain** - бизнес-логика и сущности
- **Application** - use cases и сервисы
- **Infrastructure** - реализация репозиториев, БД, внешние API
- **Presentation** - FastAPI endpoints и контроллеры

## Структура проекта

```
mind-deck/
├── domain/              # Доменный слой
│   ├── entities/        # Сущности
│   └── repositories/    # Интерфейсы репозиториев
├── application/         # Слой приложения
│   ├── use_cases/       # Use cases
│   └── services/        # Сервисы
├── infrastructure/      # Слой инфраструктуры
│   ├── database/        # БД и миграции
│   ├── repositories/    # Реализация репозиториев
│   └── services/        # Внешние API
├── presentation/        # Слой представления
│   ├── api/             # FastAPI endpoints
│   └── schemas/         # Pydantic схемы
├── frontend/           # React фронтенд
│   ├── src/
│   │   ├── components/  # React компоненты
│   │   ├── pages/       # Страницы
│   │   └── services/    # API клиенты
└── tests/              # Тесты
```

## API Документация

После запуска приложения доступна Swagger документация:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## Тестирование

```bash
# Запустить все тесты
pytest

# С покрытием кода
pytest --cov=. --cov-report=html
```

## Технологии

### Бэкенд
- **FastAPI** - современный веб-фреймворк
- **PostgreSQL** - основная БД
- **SQLAlchemy** - ORM
- **Alembic** - миграции БД
- **Redis** - кэширование
- **FSRS** - алгоритм интервального повторения

### Фронтенд
- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **Tailwind CSS** - стили
- **React Query** - управление состоянием API
- **Zustand** - глобальное состояние
